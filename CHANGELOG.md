# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added

- Added exact Stage Context contracts and Runtime-owned durable execution evidence foundations. Stage Context snapshots bind exact stage subject, Change revision, WorkState, Knowledge, Alignment, repository, Change, Evidence, Result, Skill, Gate-feedback, coverage, staleness, and query-engine identities. Typed direct and batch queries bind canonical arguments, limits, cursors, deterministic source references, unknowns, truncation, and result digests. Execution Ledgers retain exact canonical payloads in contiguous digest chains and append only through expected-head compare-and-swap. Raw Agent Session logs are retained and reverified by bytes and digest. Immutable Run Receipt commit now fails closed until matching Ledger and raw-log evidence are durable, and restart recovery rejects corrupt or identity-mismatched state.
- Added the first production-boundary DSH Runtime vertical slice. CodeWiki now pins the complete DeepSeek Harness `0.1.0-rc.6` package family and Cordis `4.0.1` exactly, records npm integrity separately from the independently reviewed upstream source commit, and fails closed because npm publishes no source attestation. The CodeWiki-owned DSH Adapter composes one isolated no-tool Agent Session with standard AgentLoop, invariants, exact prompt and model-route bindings, cancellation, usage, output, Execution Ledger digest, and JSONL raw-session evidence. Release tooling bundles that closure into one self-contained Runtime Build artifact; content-addressed qualification, expected-generation activation, exact-build binding, authenticated empty-environment Run Process launch, terminal-result validation, quiescence, process exit, and Runtime-authored Run Receipt are proven end to end with a digest-bound replay adapter and tamper rejection. Replay remains qualification evidence rather than a user-selectable backend, and temporary Pi execution remains until broader semantic parity is proven.
- Clean-cut product ownership and execution vocabulary. CodeWiki now contains one authoritative Project Server per governed project; Project Server owns transport, AuthN, project AuthZ, canonical state, Stage Loops, Checks, Implementation Workbenches, transitions, effects, and one subordinate Runtime. Runtime owns immutable Run Requests, bounded Runs, Run Processes, content-addressed Runtime Builds, and CodeWiki-authored Run Receipts without project authority. Source and tests moved from `src/server/**` plus former project-control `src/runtime/**` into `src/project-server/**`, while former `src/execution/**` moved into the new `src/runtime/**`; public subpaths are now `./project-server` and `./runtime`. Run Request, Run, Run Process, Runtime Build, and Run Receipt replace prior execution names without aliases or compatibility exports. DSH remains an exact upstream library behind the CodeWiki DSH Adapter, and Workbench remains Implementation-only.
- Added executable exact-head Review Gates and durable canonical Review operation sequences. Review now admits Evidence and provider receipt bindings only for the exact integrated revision, runs project-owned Review Check Packs, returns each failed Check's atomic feedback to Implementation, maps passed Gates only to separately guarded delivery, and preserves state for stopped Gates without fabricating Results. Expected-head synchronization verifies the integrated Git tree before append and re-verifies every accepted operation after push. WorkState Check projection exposes Review attempts and bounded failed-Result feedback. Runtime lifecycle and Gate persistence owners move from Decision-specific filenames to stage-neutral paths without compatibility artifacts; existing Change Trace bytes remain valid while the canonical operation projection admits Review as the fourth stage.
- Added host-neutral optional Check Pack Skill loading under `.codewiki/check-packs/<stage>/<pack>/skill/<skill-name>/`. Immutable Skill snapshots validate standard Agent Skills frontmatter, immediate-parent names, complete bounded binary-safe file trees, executable modes, path confinement, one Skill per Pack, stable Pack ordering, and project-wide active-name uniqueness without executing Skill content. Working-tree and exact protected-head loaders keep Skill identity separate from the Check-only `checkPackDigest`, Check Invocation/Result/Gate cache identity, and Code or Model Check inputs; a Skill-only Pack still selects zero Checks and preserves empty-Pack and no-Checks warnings. Check Pack snapshots advance cleanly to `2.0.0`; Change Trace semantic-digest validation accepts the new field while retaining historical policy digests.
- Added host-neutral producer Skill bindings and receipts, exact Pi SDK and Pi process activation, and Skill-bound Implementation Worker Assignment `2`. Runtime binds protected Implementation Skill digests into Assignment identity and rejects changed worktree guidance before process launch; Worker reports persist the same receipt. Native Decision production receives its exact protected Decision snapshot. SDK sessions materialize validated files into private temporary roots, preserve executable modes, load only explicit Pack Skills in stable Pack order, admit their roots through the read-only boundary, and remove them on disposal. Process workers disable ambient Skills, extensions, prompts, themes, and context discovery before passing explicit `--skill` paths. Skill `allowed-tools` metadata never expands explicit SDK tools or Runtime process policy, while Code and Model Check contracts remain Skill-free.
- Established Runtime-owned Loop Exit orchestration under `src/project-server/loop-exit/**`. One common router now reduces `pass | fail | indeterminate` for Decision, Planning, and Implementation, while Verification retains Check, policy, Result, Exit Report, and repair mechanics and each Loop retains Candidate and Check semantics. Native Decision Check orchestration and security policy assembly moved from `src/decision/exit/**`; the test-only Decision exit barrel, old paths, `ExitRuntime` vocabulary, aliases, and compatibility exports were deleted. Existing Decision report and route bytes, identities, operation order, Custom Check execution, research, and security behavior remain unchanged; source and test counts remain flat.
- Extracted the deterministic Alignment retrieval Benchmark harness from `src/benchmarks/**` to repository-root `benchmarks/**`. Benchmark TypeScript remains checked with product source and focused proofs remain under `tests/benchmarks/**`, but production build and npm packaging exclude all Benchmark code. Snapshot binding, adapter behavior, method ordering, metrics, error visibility, and report digests remain unchanged. No production Benchmark root, compatibility import, duplicate harness, or packed `dist/benchmarks/**` artifact survives; production source count drops by two while tests remain flat.
- Moved structured configuration errors from `src/error-handling/config-errors.ts` to `src/project/config-errors.ts`, their semantic Project Configuration owner. Error codes, class identity, messages, structured path/value data, causes, recoverability, suggested action, and all callers remain unchanged. No alias, old-path export, compatibility barrel, duplicate class, or packed legacy artifact survives; the shared error foundation now contains only generic envelopes and operation contracts.
- Deleted legacy `src/loops/**`, `tests/loops/**`, and `src/semantic-loop.ts` without aliases, compatibility barrels, old-path exports, or packed artifacts. Shared quality graph, declarative pack, activation profile, bounded runner, judge, prompt, provider-resolution, and evaluator mechanics now live under `src/verification/quality/**`; the Implementation-only quality feedback reducer lives at `src/implementation/quality-feedback.ts`; and the closed semantic Loop discriminator now belongs to Verification contracts. Decision, Planning, and Implementation retain Candidate-specific Check composition and interpretation; Runtime Loop Exit owns authoritative routing. Focused quality proofs moved under Verification, deterministic graph and judge behavior and persisted identities remain unchanged, source count drops by one, and test count remains flat.
- Deleted generic `src/views/**` and `tests/views/**` ownership without aliases, shims, barrels, or packed artifacts. Deterministic blocker, conflict, quality-readiness, trace-goal, trace-board, work-plan, and work-queue reductions now live with WorkState; status, resume, trace-queue, trigger, and runtime-board reductions live with Runtime queries. Mixed View contracts were split between those owners while preserving root package type names and projection output shapes. Removed the unexported test-only disposable View writer and its sole proof instead of carrying dead production code; `.codewiki/views/` remains valid disposable product vocabulary. Source and test file counts remain flat.
- Consolidated canonical Change Trace protocol, JSONL encoding, Change-backed storage, manifests, reduction, replay, synchronization support, retention, and specialized errors under `src/changes/trace/**`; moved graph projection, Knowledge augmentation, and bounded queries directly to `src/alignment/**`. Deleted `src/change-trace/**`, `src/traces/**`, `src/error-handling/trace-errors.ts`, and `tests/traces/**` without aliases, shims, old-path barrels, or package artifacts. Focused tests now follow their semantic owners, root exports preserve the same public names through owner-direct paths, and source/test counts remain flat. Frozen Change Trace v1 fixture payloads retain their original content-addressed historical source references and identities; those strings are canonical product data, not surviving module paths.
- Moved the deterministic CLI and its focused tests to `src/clients/cli/**` and `tests/clients/cli/**` without compatibility paths. Pi Client registration now consumes narrow dashboard and project-service ports instead of importing Server App or Runtime/Execution process lifecycle implementations. Neutral `src/pi-extension.ts` alone composes Server App and Preview lifecycle, the Runtime gateway, and the concrete Execution spawner; explicit dashboard stop, session shutdown, RPC, and multiprocess behavior remain unchanged.
- Deleted the transitional `src/api/**` root without aliases, shims, barrels, or old-path exports. Change and Decision commands now live with their domain owners; Planning, Implementation, archive, work, and state operations live under Runtime commands or queries; the OKF operation lives with Knowledge; and the default Loop execution-port binding lives with Runtime coordinator. `@nunomoura/codewiki/runtime` is the explicit operational package surface, while the root package is owner-direct and contract-focused. Pi-specific coordinator daemon composition moved from Client to Managed Execution, generic Runtime startup now fails closed without an injected spawner instead of resolving a concrete Pi implementation, and neutral `src/pi-extension.ts` Package bootstrap wires Client registration to the Runtime connection boundary and concrete Execution spawner. Generic command failures now use operation-error ownership and domain vocabulary instead of the deleted API vocabulary while preserving existing messages and error codes. Shared error envelope, serialization, type guards, and operation-failure contracts remain in a lean Package-owned `src/error-handling/**` foundation; only configuration and Change Trace specializations remain explicit owner-colocation debt. Focused tests moved with their owners, duplicate state-query coverage merged, stale packed lifecycle cleanup imports now use current Runtime artifacts, source count drops by two, and test-file count drops by one.
- Added authenticated Pairing endpoint authorization. Public issue and revoke entrypoints now require an active generation-bound Server Session, verifier-proven transition Authentication, exact active Actor identity, current Registry generation, and exact project, repository, and Runtime-route binding before a deny-by-default policy receives the fixed action and target Client. Session credentials never reach policy, invalid credentials cannot query Registry-specific binding state, and policy denial causes no transition. Direct deterministic Pairing commands remain internal but are removed from the root package API. Shared Authentication assertion provenance rejects structurally forged transition assertions. Pairing credential generation and rotation remain blocked on an approved machine credential-store contract; this cut deliberately adds no unusable, raw-persisted, or deterministic secret. Moved the five-line Runtime Decision authority declaration to the Decision Candidate proposal owner and deleted its old file, keeping source/test counts flat.
- Added provider-neutral `codewiki.server-repository-access@1.0.0` checks as a separate Server responsibility. One credential-isolated trusted adapter receives verifier-proven immutable OIDC identity, exact CodeWiki repository digest, and an opaque provider repository reference, then returns a strict short-lived `accessible | inaccessible` observation. Server validates exact identity, repository, issuer, provider, adapter, observation time, and bounded expiry and emits provenance-backed coarse evidence containing no token, role, permission, capability, Pairing, Session, Registry transition, project-operation decision, or Runtime grant. Concrete GitHub/GitLab network adapters remain pending. Deleted a production-dead Worker-observation classifier and its dedicated test, keeping source count flat while reducing test count by one.
- Added provider-neutral OIDC Authentication and immutable Actor enrollment foundations. A trusted adapter receives opaque proof and owns authorization-code exchange, PKCE S256, exact redirect validation, discovery, signature, algorithm, and key validation; Server independently binds exact Client, canonical HTTPS issuer, audience, high-entropy nonce, issued-at, expiry, adapter identity, and bounded token lifetime before deriving identity exclusively from `(issuer, subject)`. Registry clean-cuts to `codewiki.server-registry@2.0.0` with typed `local | oidc` identities and rejects legacy string-only identity arrays without compatibility parsing. Generation-bound idempotent OIDC enrollment adds only one active User mapping: no Pairing, Session, repository membership, delegation, role, project permission, or Runtime authority. Raw codes, tokens, PKCE verifiers, nonces, and mutable profile claims are not persisted. Concrete GitHub/GitLab network adapters and provider-access checks remain pending. Moved two test-only Planning and Implementation Loop declaration constants into existing owner type modules and deleted their five-line files, keeping source/test counts flat.
- Replaced the temporary personal App service binding with executable local Authentication, Pairing, and Registry resolution. Default App launch now verifies a fresh ephemeral proof through a local Authentication adapter, persists one stable local User identity, project-bound App Pairing, and exact canonical project route in private machine-level Server state, resolves the current Registry generation, and only then opens the App Session. Revoked Pairings, disabled records, identity drift, repository drift, and stale Registry generations fail closed without reactivation. Registry state contains no proof, credential, Runtime role, or project authority; injected Session bindings remain available only as an explicit composition seam. Added isolated lifecycle and packed-install coverage. Deleted the three-line `src/utils/time.ts` one-consumer abstraction and moved `IsoTimestamp` to the owning legacy Trace type module, keeping source and test counts flat.
- Wired Browser App transport through Server Session authorization. Launch URLs now carry a generation-bound `#session=` credential instead of a query token; same-origin `/api/session` establishment exchanges it for an `HttpOnly; SameSite=Strict` cookie, and every App projection, event stream, Preview command, metadata request, and shutdown request passes exact repository-bound endpoint policy before dispatch. Bounded authenticated Actor and Client context crosses the Server-Runtime gateway on every Runtime projection and remains bound independently to each event stream. Session credentials no longer appear in App API URLs or browser session storage, conflicting header/cookie credentials fail closed, and Server revokes the active Session during App shutdown. Callers may supply an Authentication/Registry-resolved App binding and endpoint policy; the later local Pairing slice replaces the temporary bounded local service binding. Merged the seven-line `src/project/config-digest.ts` helper into canonical `src/project/config-file.ts` without a compatibility path, keeping source and test counts flat.
- Added Server-owned `codewiki.server-session@1.0.0` contracts and lifecycle under `src/project-server/sessions/**`. One temporary Session binds exact actor, Client transport, project, Runtime route, generation, digest-only credential, issuance, update, expiry, and revocation state. Open, rotation, revocation, and endpoint authorization reject unsupported authority fields, use generation CAS and constant-time credential comparison, keep raw credentials out of state and policy callbacks, and fail closed on expiry, drift, repository mismatch, policy denial, or adapter failure. Successful endpoint policy returns bounded `ClientServerRequestContext` plus policy-adapter identity; Runtime still owns exact project-operation authorization. Deleted compatibility barrels `src/api/views.ts` and `src/api/traces.ts`; consumers now import projection, Trace, and Runtime persistence owners directly, keeping file counts flat.
- Split Server Authentication proof verification from Client Pairing transitions without compatibility exports. `src/project-server/authentication/proof.ts` now owns strict transient proof requests, trusted adapter assertions, normalization, and verification; `src/project-server/pairing/commands.ts` owns only generation-bound issue/revoke enrollment transitions; and `src/project-server/registry/state.ts` retains persistence and connection resolution while consuming the Authentication assertion contract. Root package exports remain stable through direct owner exports, packed declarations prove Pairing no longer exposes Authentication APIs, and the one-line `src/api/wiki-config.ts` compatibility path is deleted in favor of the canonical `src/project/config.ts` owner.
- Isolated CodeWiki Server from Runtime persistence, query implementations, canonical Trace/Knowledge reads, and coordinator internals behind `src/project-server/gateway.ts`. Authenticated Runtime RPC now exposes generation-checked, lease-bound App, Change, and configuration projections; co-located Server mode consumes the same bounded projection gateway and Runtime-owned change subscription. Preview control owns loading its project context, so no raw Trace records cross the Server boundary. Source architecture now permits Server to import only the Runtime gateway, while connected remote mode performs no machine-local canonical-state reads or watchers. App pull queries, Preview commands, SSE refresh, Runtime events, reconnect, heartbeat, and shutdown remain intact.
- Clean-cut CodeWiki Server ownership from `src/host/**` and `tests/host/**` to `src/project-server/**` and `tests/project-server/**`, including App transport, installed-runtime checks, Client Pairing, Server Registry, all direct consumers, scripts, architecture declarations, and packed artifacts. Protocol identities are now exclusively `codewiki.client-pairing@1.0.0` and `codewiki.server-registry@1.0.0`; shared Pairing message normalization lives in `src/protocol/client-pairing.ts`, while Server owns proof verification and registry transitions. Exported constants, types, commands, normalizers, connection resolution, state-root vocabulary, validation labels, and tests have no old aliases or dual parsers. Deleted misclassified `host-errors.ts`, merged its still-used execution-data projection into the generic CodeWiki error owner, and clean-cut the active completion field from `hostError` to `executionError` without dual serialization. Subsequent Server-Runtime isolation and Authentication/Pairing ownership slices complete those boundaries.
- Clean-cut the shared Client-Server wire contract from `src/api/protocol.ts` to `src/protocol/client-server.ts` and from `tests/project-server/api-protocol.test.mjs` to `tests/protocol/client-server.test.mjs`. Protocol identity is now exclusively `codewiki.client-server@1.0.0`; exported constants, types, normalizers, transport deduplication, validation labels, consumers, and packed declarations use Client/Server vocabulary with no old ID, parser, path, alias, or re-export. Generic API input validation now remains with the API error owner rather than entering Protocol. Root package access and fail-closed envelope semantics remain unchanged; the later Server ownership cut completes Client Pairing and Server Registry naming.
- Replaced the mixed public `@nunomoura/codewiki/coordinator` package with the curated `@nunomoura/codewiki/runtime` facade. `src/project-server/index.ts` exports only a narrow Project Server gateway for bounded state, inspection, Decision attention, exact Decision selection, Candidate submission, cursor-bound events, heartbeat, disconnect, and shutdown. Generated public declarations contain no Coordinator vocabulary and expose no scheduler, Worker dispatcher, Integration, effect, Loop-binding, or OCI adapter primitives. Host App coordinator observation now uses this gateway, and source architecture forbids Host transport from importing `runtime/coordinator/**`. Deleted both the Host package barrel and duplicate Runtime coordinator entrypoint without aliases; packed tests prove the old subpath is unavailable. The later Server-Runtime isolation cut moves App projections behind authenticated and co-located bounded gateway methods, completing this dependency boundary.
- Ratified the next architecture clean cut around User, Actor, User Interface, Client, CodeWiki Server, Project Server, Worker, Assignment, Workbench, and Model Provider. Server and Runtime are now desired-state siblings connected through a narrow Runtime gateway; Authentication, Pairing, Session, and Runtime Authorization are separate responsibilities. Knowledge now defines Actor Profiles, scoped Authority Grants, Claims, immutable review operations, Contribution Routing, provider-neutral OIDC with GitHub/GitLab first, exact PR feedback and delivery boundaries, `src/protocol/**`, `src/project-server/**`, curated `src/project-server/index.ts`, the future `@nunomoura/codewiki/runtime` export, dependency direction, and canonical `.codewiki/**` layout. Historical Host-named protocol IDs, paths, manifests, and checkpoints remain explicitly preserved as implementation history while `REFACTORING_PLAN.md` tracks their breaking migration without aliases.
- Added the `codewiki.host-pairing@1.0.0` authentication and pairing transition boundary. A named trusted adapter receives transient opaque proof and returns only a strict Client-kind, Client-instance, authentication-reference, and authenticated-identity assertion; caller actor, authority, role, Runtime time, credential, and effect fields fail closed. Issuance derives the accountable actor only from an active registry identity mapping, binds exact registry generation, rejects duplicate pairing, authentication-reference, and active Client-instance mappings, uses a Host-owned clock, and derives optional expiry from a bounded lifetime. Revocation requires fresh trusted authentication mapped to the pairing owner plus exact pairing/authentication-reference CAS. Raw proof is never persisted, both transitions preserve history and advance one registry generation, and the registry writer remains the sole persistence boundary. Deleted the orphan App daemon entrypoint; App development already composes the Host server directly. Concrete authentication adapters, credential storage, actor enrollment, pairing endpoint authorization, delegation, Host supervision, endpoint binding, reconnect, and delivery remain pending.
- Added the `codewiki.host-registry@1.0.0` machine-level Host state foundation. One strict bounded snapshot records stable user or service actors, opaque authenticated-identity references, paired Client instances, repository identities, normalized project roots, and per-project Runtime route references. Private canonical JSON persistence uses one no-follow read descriptor, an exclusive writer lock, atomic replacement, directory synchronization, and expected-generation CAS. Append-preserving transitions prevent stable actor, Client proof, or project-route reassignment and disabled/revoked reactivation while requiring audited update-time advancement. Resolution consumes a trusted authentication-adapter assertion rather than a caller actor ID, binds the exact expected registry generation, and fails closed for malformed, non-private, oversized, stale, future-dated, ambiguous, disabled, revoked, expired, mismatched, or unknown state. Registry state contains no credentials or delegation grants and grants no Runtime authority. Concrete authentication adapters, credential storage, actor enrollment, pairing endpoint authorization, delegation authorization, Host process supervision, endpoint binding, reconnect, and delivery remain pending.
- Added strict `codewiki.host-client@1.0.0` command, bounded-query, durable-operation, and projected-event envelopes. Host-attached request context now separates accountable `actorId` and proof-backed `authenticatedIdentityRef` from `clientKind`, paired `clientInstanceId`, transport `authenticationRef`, and optional explicit `delegationRef`; the same actor retains identity across App, CLI, Pi, MCP, and channel Clients. Commands bind repository, target, expected digest, actor-scoped semantic idempotency, expiry, requested capability, and bounded payload while rejecting caller authority, Runtime time, route, Verification outcome, and effect receipt fields. Client-instance transport deduplication and Runtime semantic idempotency have separate content identities. Change Trace Protocol `3.0.0` and Provider Check Receipt Adapter `2.0.0` clean-cut ambiguous legacy identity vocabulary to authenticated identity without aliases or compatibility parsing. Host authentication adapters, delegation authorization, and endpoint wiring remain pending.
- Ratified target source architecture before further feature work. Decision, Planning, and Implementation own Loop-specific Candidate, Check, attempt, and interpretation semantics. Runtime owns common Loop Exit orchestration and authoritative routing alongside scheduling, persistence, synchronization, claims, workers, Integration, recovery, and effects; it cannot grow separate Loop policy packages. Shared `src/verification/**` replaced the former mixed shared `src/loop-exit/**`; canonical Change, WorkState, and Alignment homes will be `src/changes/trace/**`, `src/work-state/**`, and `src/alignment/**`. The plan now names legacy Quality, Trace, ChangeRecord, WorkState, View, Loop-named Runtime, and stale source-layout paths as explicit deletion/rehome debt, with no aliases, dual paths, or old-path re-exports. Added internal source-architecture manifest and regression gate to freeze current roots, legacy-file counts, Runtime-to-Pi import debt, and import cycles while clean cuts remove them.
- Added bounded production Decision research collection through `codewiki.decision.research-collection@1.0.0` and advanced Pi Native Decision Host to `2.0.0`. One trusted-host collector receives an exact Candidate-, collector-configuration-, sensitivity-, citation-count-, receipt-byte-, and timeout-bound request. Runtime validates exact request echo and receipt status, owns observation time and freshness, materializes observed Change-revision citation Evidence, and invokes isolated claim-support assessment only after provenance passes. Collected citations and assessments commit with the canonical Decision continuation; exact replay skips collection and model work. Partial, unavailable, malformed, timed-out, contradictory, mixed-freshness, or wrong-subject input remains `indeterminate`. Networking and credentials stay in the trusted host, and caller-owned research freshness is rejected without a compatibility path.
- Added `codewiki.custom-check-evaluator@1.0.0` and advanced Decision Model Check requests to `5.0.0`. Each Custom Model Check now binds the exact Candidate, Check, definition, protected source/config snapshot, accepted User Standard snapshot and selected passage text, considered Evidence, prerequisite Results, no-tool route, and evaluator configuration in a fresh no-shared-state request. One response must echo that binding and supplies bounded Evidence gaps, counterevidence, coverage, truncation, and repair targets. Evidence schema `1.4.0` records request and Assessment digests plus evaluator identity, while Loop Exit Runner `1.3.0` replaces split Evidence/Result callbacks with one atomic materialization boundary. Mismatched identity, route/configuration replay, prerequisite drift, malformed output, and oversized repair remain `indeterminate`.
- Added independent high/critical Decision assurance and separately authenticated residual-risk authority. Check Catalog `10.0.0` activates `security_independent_challenge_reviewed` on a distinct no-tool provider/model route and `security_residual_risk_authorized` only for exact high/critical Candidate risk. Evidence schema `1.3.0` scopes every approval receipt to one Check/version/purpose. Residual-risk receipts require a qualified security/risk role, an authenticated identity distinct from general Candidate approval, both complete supported Candidate-bound challenge assessments from distinct routes/configurations/producers, exact risk, prior approval identity, rationale digest, and optional finding digests. Missing, disagreeing, unavailable, aliased-route, generic/same-authority, or wrong-Candidate input remains `indeterminate`; scanner and model output never authorize risk.
- Added off-repository `codewiki.security-route-calibration@1.0.0` admission and scoring. Sealed human-labeled route receipts must bind exact Scanner Suite `3.0.0` and atomic evaluator `2.0.0` identities; provide pass, defect, and unavailable cases for all six scanner families with a critical trap; and bind source/report/request/environment/configuration/scanner/evaluator/Evidence identity, limitations, latency, and cost. Reports separate false passes, false failures, escaped critical defects, `indeterminate`, latency, and cost per route. Incomplete matrices, protocol drift, false passes, escaped critical defects, and below-threshold scores fail closed. Lab calibration grants no Evidence, Result, promotion, or effect authority; real sealed execution still requires external human cases and production scanner receipts.
- Added fixed Gitleaks directory SARIF collection and closed scanner-family vocabulary. `codewiki.production-security-collector@2.0.0` now binds exact Gitleaks executable/version, rules bytes, ignore-policy bytes, Candidate source/tree/environment, fixed redacted no-shell invocation, bounded output/time, and pre/pre-scan/post-run identity checks without ambient configuration, archive recursion, decode recursion, credentials, plugins, or network update. Scanner Suite `3.0.0` clean-cuts generic `configuration` to `infrastructure_configuration` and closes the six measured families at static analysis, dependency advisory, secret detection, infrastructure configuration, authorization testing, and migration testing. `codewiki.atomic-security-scanner-check@2.0.0` and Check Catalog `9.0.0` add independent `infrastructure_configuration_verified` activation. Parsing, process, filesystem, cryptography, public-interface, and browser-trust concerns remain in applicable static-analysis, standard contract/test, or independent Model Checks instead of creating scanner families without distinct measurements.
- Added `codewiki.atomic-security-scanner-check@1.0.0`, advanced the Scanner Suite to `2.0.0`, and clean-cut Check Catalog to `8.0.0`. Exact scanner-family, request, and outcome provenance now lets one immutable `security_scanners_valid` Evidence substrate feed five protected Decision Code Checks: `static_analysis_findings_absent`, `dependency_advisories_absent`, `credential_exposure_absent`, `authorization_controls_verified`, and `persistence_safety_verified`. Surface activation selects only relevant families. Each Check depends on the suite producer, filters only its scanner-family/request records, independently resolves command/source obligations, and receives a separate Runtime-created Result. One family can pass while another fails or remains `indeterminate`; findings stay fail-dominant, while missing, conflicting, partial, stale, or execution-error Evidence cannot pass. Exact replay reuses the same Evidence identities, the runner records shared records once, and scanners/evaluators retain no Result or acceptance authority.
- Added `codewiki.production-security-collector@1.0.0` with fixed Semgrep SARIF static-analysis and offline Trivy filesystem/advisory SARIF profiles. Collectors bind exact executable bytes and reported version, Semgrep configuration or Trivy database digest, Candidate source snapshot/tree/environment, fixed invocation identity, and a closed credential-free process environment. Runtime uses structured no-shell arguments, disables metrics/version checks or database updates, bounds probe/scan time and combined output, rechecks executable/configuration/database bytes before and after execution, routes accepted output through the bounded SARIF adapter, and emits sanitized scanner observations/intake. Missing or changed files, version drift, stale/mismatched advisory identity, cancellation, timeout, output overflow, nonzero exit, malformed SARIF, and post-run identity drift remain unavailable or partial Evidence. No arbitrary command, arguments, shell, plugin, implicit network update, raw finding detail, authority, or Result enters the collector contract.
- Added `codewiki.standard-evidence-check-executor@1.0.0` and advanced it to `1.1.0` with exact shared-substrate fan-out. Trusted hosts may supply at most 64 exact report-bound capabilities; each binds one Loop/Check/version, all of that Check's Evidence obligations, one normalized closed selector, one verified adapter receipt, and one materialized bundle. The executor requires the same selector in protected Resolved Exit Policy parameters, rechecks exact Candidate/source identity, binds receipt and bundle digests into execution configuration, and maps deterministic observations into boolean Code Check output. Multiple independent Checks in the same Loop may consume one byte-identical bundle and receive separate resolutions and Runtime-created Results. Loop Exit Runner `1.2.0` records or streams each identical Evidence Record once, reuses already supplied exact records on replay, and rejects same-id content conflicts. Report-bound Result caching remains disabled; cancellation, mismatches, duplicate Check capabilities, inconsistent shared bindings, unknown obligations, non-Code Checks, and permissive obligations fail closed. Check Catalog advances cleanly to `7.0.0`; standard external-command obligations require artifacts. This adds no central Evidence store, and adapters/evaluators still grant no Result.
- Added closed `codewiki.standard-evidence-check-evaluation@1.0.0` for deterministic interpretation of exact complete core-format Evidence. Five protected selector families cover JUnit minimum-test, zero-failure/error, and maximum-skip policy, LCOV/Cobertura line/branch/function thresholds in integer basis points, SARIF blocked finding levels, authenticated provider accepted conclusions, and required identity presence in CycloneDX/SPDX/Pact/OpenAPI artifacts. Evaluation binds exact adapter receipt/bundle/protocol, declarative obligation resolution, and selector digest; rejects partial-permitting obligations, source/bundle drift, wrong adapter families, unsupported selector fields, and empty quantitative denominators; and returns only `satisfied | unsatisfied | indeterminate` observations plus bounded factual counts. It does not create a Check Result. SBOM identity readiness remains distinct from security/license claims, and contract identity readiness remains distinct from verification/conformance.
- Added `codewiki.evidence-adapter.materialization@1.0.0`, the closed Runtime bridge from accepted core adapter receipts to canonical Evidence Records and exact obligation readiness. It verifies receipt integrity, supported adapter protocol, source-snapshot/subject equality, material/artifact binding, and authority ceiling; fixes producer, coverage, freshness, sensitivity, and protocol/receipt/binding provenance; and emits immutable command/source Evidence identities with `grantsResult: false`. Exact accepted-protocol reduction uses existing declarative obligations: complete observations become ready inputs even when their factual outcome is failing, while partial, unavailable, stale-source, tampered, wrong-protocol, or duplicate Evidence remains missing or `indeterminate`. Runtime Check implementations still own semantic `pass | fail | indeterminate` reduction. SARIF, JUnit, LCOV, and Cobertura receipts now expose the same explicit source snapshot, authority ceiling, and no-Result fields as the other core adapters. Obligation validation now correctly preserves eligible neutral Evidence as potentially relevant rather than contradicting reducer output.
- Added bounded `codewiki.evidence-adapter.cyclonedx@1.0.0`, `codewiki.evidence-adapter.spdx@1.0.0`, `codewiki.evidence-adapter.pact@1.0.0`, and `codewiki.evidence-adapter.openapi@1.0.0`. Exact Runtime bindings cover source snapshot, evidence scope, 1..256 project-relative source paths, up to 512 required identity digests, tool/version, request, invocation, environment, configuration, execution, ownership, and provenance. A shared 4 MiB, 64-depth, 100,000-node document boundary rejects malformed UTF-8, duplicate JSON/YAML keys, aliases, anchors, tags, unsafe paths, excessive nesting, unsupported profiles, and caller authority/Result fields. CycloneDX 1.7 JSON preserves bounded component/service/dependency/vulnerability/composition facts; SPDX 2.3 JSON preserves package/file/snippet/relationship/license/external-document facts; Pact 4.0 JSON preserves consumer-provider interaction structure without claiming verification; OpenAPI 3.0.0..3.0.4 and 3.1.0..3.1.1 JSON/YAML preserve operations, callbacks, webhooks, components, security schemes, and unresolved references. Names, package locators, contract bodies, API paths, and private values remain artifact-bound and digest-only. Missing required identities, truncation, declared incomplete composition, unresolved external documents/references, timeout, cancellation, or unavailable collection remain partial or unknown. Every adapter emits bounded collection `command_execution` and contract/inventory `source_observation` material with `authorityCeiling: observed` and `grantsResult: false`; no SBOM, Pact, or OpenAPI artifact grants a vulnerability, verification, compatibility, conformance, approval, or exit Result. SARIF ingestion now reuses the bounded JSON boundary to reject duplicate keys and excessive document structure.
- Added bounded `codewiki.evidence-adapter.provider-check-receipt@1.0.0` for authenticated provider Check observations, since advanced to `2.0.0`. A trusted connector must emit one digest-only canonical JSON receipt bound to exact provider instance, repository, source snapshot, Git head, Check identity/configuration, authentication method/identity/credential identity, adapter/version, and retrieval request. Ingestion accepts at most 64 KiB, rejects duplicate keys, non-canonical JSON, caller authority, credentials, context drift, unsupported protocol fields, and contradictory state/timing. Provider Check and payload identities, attempts, annotation counts, timestamps, and provider conclusions remain factual observations; completed success and failure receipts are both complete. Queued/in-progress receipts remain partial and unavailable retrieval remains unknown. Output is authenticated-retrieval `command_execution` material with a `verified` authority ceiling and `grantsResult: false`; it does not convert provider conclusions into CodeWiki Results, approvals, Integration proof, delivery authority, release, or deployment authority.
- Added bounded `codewiki.evidence-adapter.lcov@1.0.0` and `codewiki.evidence-adapter.cobertura@1.0.0`. Runtime binds exact source snapshot, coverage scope, up to 255 required project-relative paths, tool/version, request, invocation, environment, configuration, and execution before ingesting at most 4 MiB. LCOV parsing accepts closed detailed line/branch/function records, merges duplicate source records, derives hit counts, and cross-checks declared totals. Cobertura parsing reuses the safe bounded XML boundary, derives counts from class/method/line data, validates branch count percentages, and cross-checks root totals without trusting reported rates. Unsafe or missing paths, declaration drift, more than 2,048 unique files, malformed records, timeout, cancellation, or unavailability remain partial or unknown. Raw function/class identities stay digest-only. Output contains factual count summaries plus `command_execution` and `source_observation` Evidence material; low coverage may be completely observed but never grants a threshold Result or exit authority.
- Added bounded `codewiki.evidence-adapter.junit@1.0.0` for common JUnit XML `testsuites` and `testsuite` reports. Runtime must bind exact runner/version, source snapshot, test selection, expected test count, request, invocation, environment, configuration, termination, exit code, and duration. Audited XML parser/validator dependencies reject malformed syntax, DTD/entity/processing declarations, invalid UTF-8, and nesting beyond 32 under a 4 MiB artifact limit. Ingestion admits at most 256 suites, processes at most 8,192 test cases, retains at most 256 summary/non-passing diagnostic refs, hashes case identity and private failure/error detail, and treats expected/declared count drift, unsafe file paths, truncation, non-exit, or unavailable execution as partial or unknown coverage. Output is only factual aggregate metadata and `command_execution` Evidence material with exact provenance; test outcomes never grant a Check Result or exit authority.
- Added bounded `codewiki.evidence-adapter.sarif@1.0.0` for SARIF 2.1. Runtime bindings fix expected tool name/version, source snapshot/scanned paths, adapter/version, request, invocation, environment, configuration, optional advisory database, termination, and duration before ingestion. The adapter hashes at most 4 MiB of exact artifact bytes, accepts at most 32 runs, processes at most 8,192 findings, retains at most 256 compact observations/refs, rejects tool drift and caller authority fields, excludes unsafe absolute/URI locations with partial coverage, and stores message digests rather than potentially sensitive finding text. It emits only `command_execution` and `source_observation` Evidence material plus a canonical receipt; Runtime still owns subject/time/producer/authority/coverage materialization, semantic reduction, Results, and exit. Non-exited, unavailable, truncated, omitted, or unsafe observations cannot become complete Evidence.
- Advanced executable `codewiki.verification-capability-matrix@2.0.0` with a clean cut to nine core interchange formats. It projects every Loop-qualified Default or active Custom Check from exact Check Catalog/config identity into deterministic execution status (`native | host_required | capability_required`), immutable Evidence obligations, current gaps, and potential standard Evidence formats. Canonical `codewiki_evidence_material` admission and all nine core interchange contracts—SARIF 2.1, JUnit XML, LCOV, Cobertura, CycloneDX, SPDX, Pact, OpenAPI, and authenticated provider-check receipts—now have bounded ingestion. Playwright uses JUnit plus CodeWiki UI captures, while axe findings use SARIF rather than separate tool-native core schemas. Every format is Evidence-only (`grantsResult: false`) and carries an authority ceiling; no artifact format, scanner, or provider receipt grants a Check Result. Current matrix truth reports nine implemented core adapters, making remaining ingestion work explicit rather than implied.
- Added authenticated Decision-attention browsing and explicit Pi user selection controls. `/v1/runtime/decision-attention` returns a canonical bounded `codewiki.backlog-triage-query@2.0.0` result: an omitted query bootstraps from the current exact projection, while supplied queries must bind that projection digest and reject drift or unsupported fields. `ProjectCoordinatorRemoteClient`, `PiProjectServiceClientProvider`, and read-only parallel `wiki_attention` expose the same result. `/wiki-attention` renders exact Change/revision/projection identities; `/wiki-select <change-id> --revision <revision-id> --projection <digest>` creates a fresh idempotency key and submits only the strict selection command. Selection remains an explicit project-local Pi user command—there is no model-callable selection tool, callers cannot add authority fields, observers remain unable to select, and absent native host capabilities fail closed.
- Added `codewiki.pi-native-decision-host@1.0.0` and `createPiNativeDecisionStartOptions()` as the default production composition for selected Decision work. Given mandatory trusted repository identity, current project authority, replay policy, and Runtime continuation authority, it assembles fresh Git triage context/attempt admission, a native Pi Candidate producer, exact protected-config Loop Exit construction, canonical continuation commit, and restart recovery. `resolvePiDecisionSelectionAuthority()` admits only approved Pi coordinator connections, derives stable actor identity and connection-specific authentication Evidence through SHA-256 without persisting bearer metadata, and supports an additional project authorization callback. Pi daemon `nativeDecision` configuration installs this bundle and is mutually exclusive with a raw injected `decisionStart`. Disposable two-clone service proof covers denial, exact selection, one producer run, complete canonical exit, daemon restart, and recovery without reinvocation. This authenticates a project-local Pi connection, not an external user/provider identity; repository authority inputs remain host-required.
- Added `createPiSdkNativeDecisionCandidateProducer()` for selected native Decision attempts. It validates the closed `codewiki.decision-candidate-production@1.0.0` request—including exact revision identity and relationship shape—before opening one isolated read-only Pi SDK session, permits exactly one strict disposition/rationale submission, enforces invocation/output/time bounds, and propagates coordinator cancellation into session abort and disposal. Authority-bearing or extra request fields are rejected before any model session starts. Existing legacy semantic adapters remain separate; the Pi native Decision host now uses this producer.
- Added `createDecisionGitAdmission()` as the Runtime-owned selection context and canonical attempt appender. It fresh-synchronizes remote Change state, loads exact protected config from the team source head, compiles Standard-derived triage policy, projects the Alignment Graph and a short-lived digest-bound Backlog Triage context, and reuses that exact context across authorization revalidation. Attempt admission re-fetches current state, checks expected WorkState and every team snapshot field, performs one expected-head Git state push without blind retry, re-synchronizes, and verifies the exact accepted operation. Repository identity, current project authority snapshot, and replay policy remain mandatory trusted host inputs.
- Added `createNativeDecisionAttemptExecutor()` as the selected-Decision execution bridge. It reloads fresh synchronized Git state, verifies the exact authenticated active attempt and current revision, requires a Loop Exit binding whose protected source/config exactly match the team snapshot before invoking a producer, sends one versioned `codewiki.decision-candidate-production@1.0.0` request containing no authority or Evidence, materializes Candidate `2.0.0`, runs native Code/Model/Evidence/Report/Route reduction, and commits the complete continuation under expected team/WorkState CAS. The executor includes supplied and newly produced Evidence, propagates cancellation, rejects stale or malformed producer/evaluation input, and recovers terminal canonical attempts without reinvoking the producer or evaluator. Pi daemon startup can accept an injected complete `decisionStart` bundle. Runtime now supplies Git context/appender assembly, Pi SDK supplies the native Candidate producer, and `createPiNativeDecisionStartOptions()` installs the default project-local approved-Pi authority and daemon composition when mandatory trusted project inputs are configured.
- Clean-cut Decision Candidate schema to `2.0.0` over native `ProjectWorkState`. Runtime now accepts only the producer's strict disposition/rationale proposal, resolves one current non-withdrawn Change, embeds the exact semantic revision and active canonical relationships, derives overlap accounting and WorkState/Knowledge/source/config/policy refs, and owns every Candidate binding. Removed legacy `ChangeRecord`, caller-supplied observed bases, copied validation/provenance/estimate fields, duplicate grounding refs, and unresolved-fact summaries without aliases. Decision Code Checks and security/research Evidence consume the canonical revision ID and acceptance requirements directly. Native continuation admission reconstructs the Candidate from current WorkState and rejects any stale or caller-expanded artifact before Git mutation. A host-configured native attempt executor now runs this path end to end; default Pi authority/context/appender/producer assembly remains pending.
- Advanced Change Trace Protocol to `2.0.0` with a breaking clean cut from skeletal revision fields to one immutable normalized semantic revision. Exact revision identity now includes intent and alternatives, Runtime-owned classification, user/maintainer/compatibility impact, Knowledge propagation, observable outcomes, delivery constraints, source/proof Evidence expectations, safety/risk/failure/rollback semantics, acceptance requirements, and the optional defect profile. Native intake deterministically classifies each of its eight authenticated source families, preserves exact observed and desired behavior, leaves unsupported assurance facts empty or unknown, and cannot promote source-claimed severity into revision risk. Triage, overlap, conflict serialization, deduplication, graph projection, canonical fixtures, and replay now consume these nested semantics directly. Removed the old `summary`, `desiredOutcome`, `constraints`, `nonGoals`, `knowledgeRefs`, `sourceRefs`, and top-level `risk` revision shape without a compatibility parser.
- Simplified authenticated exact-revision Decision attention selection through `codewiki.decision-attention-selection@2.0.0`. One strict user command now carries only an actor-scoped idempotency key, exact Change/revision identity, and the Backlog Triage Projection digest that already commits WorkState, triage Candidates, graph, protected config, and policy. Runtime resolves authority from trusted caller metadata, validates and authorizes the exact request, reloads context, then appends canonical `loop.attempt_started` before scheduling. That operation carries the authority/authentication Evidence binding, exact revision/base snapshot, and private idempotency digest; its operation ID is also the coordinator job key. Same actor/key and revision replay the operation, while changed semantic input conflicts. Removed the standalone Selection Receipt, repeated nested command/binding digests, process-local receipt memory, duplicate selection/job identities, and broad `ProjectCoordinatorDecisionAttentionSelectionAdapter` without aliases. Revision-derived Change/Knowledge/source/component refs preserve overlap serialization and canonical attempt state drives recovery. Pending Changes and generic Runtime triggers remain unable to auto-select Decision work or suppress approved Planning solely through possible overlap; generic candidate submission cannot substitute for selection. The native Candidate/evaluator/continuation core is now ProjectWorkState-bound; end-to-end selected-job host wiring remains pending.
- Added protected Standard-derived Backlog Triage policy. User Standard Distillation Protocol `2.0.0` clean-cuts ordering proposals to eleven closed dimensions. Guarded Mutation, Policy Review, and Protected Acceptance Protocols `5.0.0` plus protected configuration schema `3.0.0` atomically persist each accepted `triage_preference` as an immutable distillation/Standard/source/passage-bound `triagePreferences[]` entry. `codewiki.backlog-triage-policy@1.0.0` validates those bindings and derives fixed Runtime-owned lexicographic precedence and directions; repeated dimensions merge source refs without weight, unknown values stay behind supported values, and protected safety tiers remain first. Backlog Triage Projection and Query Protocols `2.0.0` bind the exact compiled policy/config digest and expose source-bearing ordering reasons. Models cannot author direction, precedence, weight, final rank, score, canonical queue state, Check outcomes, or Planning priority.
- Added approved-template Custom Code Checks. Custom Check schema `4.0.0` and Check Catalog `6.0.0` introduce the closed `codewiki.custom-code-template@1.0.0` / `resource_usage_limit` evaluator for exact model-token, cost, latency, changed-file, and trace-byte limits over bounded Decision, Planning, and Implementation scopes. Runtime derives immutable template/parameter/configuration identity, requires exact `codewiki.custom-code-capability-snapshot@1.0.0` model-usage, Git-change, or trace-size meter capability before activation, installs deterministic Code Check executors, and exposes matching fail-closed preflight, meter, cancellation, route-admission, and quantitative Evidence materialization. Evidence schema `1.2.0` adds candidate-bound complete-window `resource_usage`; missing, stale, duplicate, partial, wrong-candidate, or mismatched observations produce `indeterminate`, while exact over-limit observations fail independently. Distillation remains inert: `codewiki.custom-check-mutation@4.0.0` requires authenticated selection of each exact Custom Code proposal plus separately approved typed template parameters, derives all executable fields, and binds proposal, template, capability snapshot, Standard/definition changes, and protected config through Policy Review and Acceptance Protocols `4.0.0`. No user/model JavaScript, shell, prompts, tools, schemas, dependencies, regex programs, or verdict logic are accepted. Production meter collectors and full Loop scheduling remain pending.
- Added guarded atomic distilled User Standard bundle mutation. `codewiki.custom-check-mutation@3.0.0` accepts one exact completed distillation receipt plus an authenticated bounded selection of generated proposal ids, then adds the immutable Standard and selected draft Custom Checks through one complete-config CAS. Mutation authorization and content-addressed receipts bind the full distillation review bundle, exact selected ids, Standard and definition transitions, protected base, before/after config, and next-snapshot effect. Policy Review and Acceptance Protocols `3.0.0` carry those bindings through separate review `pass`, separate acceptance authority, deterministic config-only Git commit, protected-head expected-CAS push, exact re-observation, and replay. Acceptance rejects a mutation whose before-config digest is not the exact protected-base config, preventing stacked unreviewed working changes from hitchhiking into the reviewed commit. Source clauses mapped only to Default Checks, triage preferences, guards, or unresolved states may accept a Standard with zero generated Checks. Distillation still grants no activation; Standard replacement/redistillation remains pending.
- Added User Standard Source Retrieval Protocol `1.0.0` and User Standard Distillation Protocol `1.0.0`. Runtime normalizes bounded inline selections, routes exact HTTPS selections through credential-isolated read-only retrievers, preserves unavailable/malformed/provider states without leaking payloads, and materializes content-addressed source receipts. One fresh tool-free Pi distiller receives only the exact sanitized snapshot, kernel Default Check catalog, closed Check Types, route, and limits. Strict output preserves exact passages, Default Check coverage, source-bound Custom Model proposals, inert Custom Code template intents, quantitative Runtime guard requirements, deterministic triage preferences, and unresolved contradictory/stale/partial/excluded clauses in a tamper-checked review bundle. Distillation grants no activation, Result, ordering, evaluator, configuration, or Git authority; guarded mutation and protected acceptance consume only authenticated selected review bundles.
- Added source-bound User Standard foundation. User Standard schema `1.0.0` materializes immutable bounded inline or HTTPS source snapshots with exact observed time, normalized content digests, atomic passage ids, stable Standard identity, strict URI/credential/control/size checks, and tamper validation. Custom Check schema `3.0.0` clean-cuts source-unbound proposals and requires exact accepted Standard id/digest/passage refs. Project config and protected configuration schema `2.0.0` bind complete Standards and Checks; Check Catalog `5.0.0`, Resolved Exit Policy, and Decision Model Check Request Protocol `4.0.0` carry those refs into independent assessment. Guarded mutation, policy review, and protected acceptance advance to `2.0.0` and preserve Standard identity, while atomic Standard-plus-Check mutation, deterministic Custom Code templates, Runtime resource guards, and Standard-derived triage remain pending.
- Added closed `codewiki.security-scanner-suite@1.0.0` and protected Decision Check `security_scanners_valid`, advancing Check Catalog to `4.0.0`. Deterministic surface activation selects a static-analysis baseline plus bounded dependency-advisory, secret, authorization, public-interface, parser, process, filesystem, cryptography, persistence, infrastructure, and browser-trust scanner families. Strict adapter requests bind exact Candidate, source snapshot/tree, environment, adapter/configuration, source/Knowledge/ownership refs, and fresh advisory snapshots; malformed or authority-bearing fields fail closed. Runtime materializes separate observed command/source Evidence, maps findings to failed Results and sanitized `security_scanner` intake, and keeps missing adapters, unavailable execution, partial coverage, malformed output, and stale advisories `indeterminate`. Scanner Evidence and prerequisite Results flow into the independent security Model Check. Persisted scanner Evidence replays only for an exact request/source/environment/advisory match, while external-state-bound scanner executors bypass generic Result caching.
- Added `codewiki.backlog-triage-projection@1.0.0` and `codewiki.backlog-triage-query@1.0.0` under `src/changes/triage/**`. The disposable projection validates exact WorkState and Alignment Graph identities; binds remote/source/Knowledge/config/policy/graph content; includes only current pending, deferred, repair, escalation, or route-back revisions; and preserves source provenance, structured defect context, Decision readiness, protected escalation, active-work blocking, urgency, expected impact, effort, risk of inaction, implementation risk, reversibility, confidence, overlap, freshness, Pareto membership, and bounded age fairness. Every supported estimate carries exact snapshot binding, Evidence authority, analysis class, support refs, and assumptions; unsupported dimensions remain `unknown` and declared Change risk is not repurposed. Default ordering uses six explicit reasoned tiers without an overall score or priority. The shared strict query supports closed filters and ten ordering families, returns at most 100 candidates with ordering reasons and coverage, and rejects stale/tampered identity, unsupported DSL or priority fields, duplicate filters, and unsafe bounds. Backlog UI remains deferred to the final Dashboard phase.
- Added closed Change intake producers for user suggestions, provider-neutral pull-request findings, Worker Report discoveries, regressions, security scanners, delivery/outcome Evidence, and Knowledge drift. Change Intake Material Protocol `1.1.0` adds bounded normalized claimed-security metadata, and Change Trace Protocol `1.3.0` binds those exact bytes during replay. Producers inject only exact source bindings and reuse the shared strict material normalizer; they reject unsupported authority or scheduling fields. Delivery and outcome adapters validate exact Evidence Records, Knowledge drift binds exact linter issue/snapshot identity, and security findings preserve normalized qualified references. Pi process Worker Reports now parse, validate, persist, and recover up to sixteen bounded discovery proposals; Runtime must add exact Worker Report, Assignment operation, Work Item Claim operation, and base/result tree bindings before those proposals become intake material.
- Added Change Defect Profile Protocol `1.0.0` as an optional identity-bearing part of a Change revision. Closed category, severity, likelihood, exposure, confidence, reproducibility, regression, affected-version/tree/component, behavior, source-location, rule, provenance, and security-reference fields preserve qualified SARIF 2.1, CWE, CVE, GHSA, OSV, CVSS, and CISA KEV metadata without treating identifiers or source/model claims as verified vulnerability facts. The profile deliberately excludes risk and priority, rejects credential-like or malformed content, keeps unknown dimensions explicit, and binds Evidence authority and refs. Change Trace Protocol `1.2.0` carries the profile in revision identity; authenticated intake creates only asserted profiles, leaves likelihood/exposure/risk unknown, and cannot promote claimed severity into authority.
- Added Change Intake Material Protocol `1.0.0` as a strict closed union for authenticated user suggestions, provider-neutral pull-request findings, Worker Report discoveries, regressions, security scanners, delivery observations, outcome findings, and Knowledge drift. Source-specific exact bindings and a bounded semantic core normalize text to NFC/LF, sort exact refs, reject unsupported or authority-bearing fields, malformed digests/Git objects, credentials, controls, duplicate refs, and inputs above 16,384 UTF-8 bytes. Change Trace Protocol `1.1.0` now permits that exact material on proposal and feedback operations and rejects noncanonical or identity-tampered inline bytes during replay. Runtime verifies authenticated source Evidence, validates exact WorkState correlation, stores complete normalized material as an identity-checked inline operation artifact, records durable request/source/semantic fingerprints, replays exact accepted requests, reinforces deterministic source or semantic matches, distinguishes current feedback from linked independent discovery, and admits typed operations through fresh expected-head Git CAS with post-push verification and no blind stale retry. Removed legacy `user | runtime | lab` feedback intake, its old Change-record deduplication helper and Lab adapter, and the special `wiki_change intake` path without an alias or compatibility parser. Source producers and client transport remain pending. The final Dashboard refactor is now sequenced after Runtime, assurance, archive, projection, and legacy Trace clean cuts, immediately before external release proof.
- Added protected Custom Check policy review and acceptance protocols `1.0.0`. Runtime authenticates one exact three-valued review receipt, requires `pass` plus separate authenticated acceptance authority, binds repository/protected-ref/config identity, and creates a deterministic child commit that changes only `.codewiki/config.json`. A safe configured Git remote receives that exact commit under expected-head `--force-with-lease`; Runtime re-observes the accepted ref/config, serializes final config revalidation against concurrent mutations, rejects stale or drifted attempts without blind retry, and idempotently recognizes an already accepted exact commit. Commit history binds mutation, review, acceptance-intent, and config identities without granting review model output Git authority.
- Added guarded Custom Check configuration mutation protocol `1.0.0`. Strict create, update, activate, and disable commands bind authenticated authority and an idempotency key to exact current/protected project-config digests and protected Git source head; Runtime authorizes the exact semantic before/after change and emits a content-addressed receipt effective only from the next protected snapshot. A locked atomic project-config adapter verifies complete-config CAS, while an exact-commit loader keeps Resolved Exit Policy and native Decision assurance on protected-base definitions so policy changes cannot disable their own Checks. Raw Custom Check arrays are removed from Runtime/policy inputs. Decision Model Check Request Protocol `3.0.0` is now identified as `codewiki.decision.model-check-request` and carries protected source/config snapshot bindings for each Custom Check. Dashboard/CLI/API transport, hosted-provider review publication, and policy-change review UI remain pending.
- Added the executable Custom Check contract/config/catalog/policy foundation. Ten closed Check Types constrain bounded project requirements; Runtime materializes stable ids and semantic definition digests, separates exact `draft | active | disabled` lifecycle through a Custom Check configuration digest, normalizes and rejects tampering, and persists only complete definitions. Check Catalog `3.0.0` emits every active definition as a required project-authority Model Check; policy applies exact loop/Change-kind/layer/path applicability and binds type/evaluator/Knowledge metadata; active `fail` or `indeterminate` Results block exit; and Decision Model Check Request Protocol `2.0.0` transports exact Custom Check definition context through focused calls. Removed broad `ProjectCheckRegistration`, integer Custom Check revisions, enforcement-stage rollout, and definition-owned approval without aliases or dual paths. Dedicated Dashboard commands/UI, type-route binding, Knowledge-content resolution, batching calibration, and production cutover remain pending.
- Ratified project-bound Custom Checks under closed CodeWiki-owned Check Types. Target design uses bounded declarative requirement text and applicability, protected Git-backed config, Dashboard-first lifecycle and agent-feedback preview, type-specific Check Evaluators, separate model Assessments and Runtime-owned Results per Custom Check, required applicable active Checks, protected-base anti-self-disable policy, and calibration-gated focused or type-batched model calls while preserving Code Check and Model Check execution kinds.
- Added deterministic Decision security-surface activation and structured security challenge review. Runtime now classifies twelve closed surfaces from the exact semantic revision, binds the content-addressed classification and refs-only Knowledge/source coverage into policy identity, activates targeted dependency/public-API/persistence Checks, and requires `security_surface_requirements_complete` before model review. Check Catalog `1.1.0`, Evidence schema `1.1.0`, and Decision Model Check Request Protocol `1.1.0` standardize exact considered-Evidence echo plus positive/negative/uncertain basis validation; security challenge output retains bounded threat goals, preconditions, attack paths, violated invariants, Candidate/Evidence refs, claimed severity/confidence, mitigations, and limitations as asserted Evidence while Runtime alone derives Check Results and deterministic Exit Reports.
- Added bounded inline semantic artifacts and synchronized native Decision admission. Candidate, Resolved Exit Policy, Evidence Record, Check Result, Exit Report, and Runtime Route bytes now live in their typed Change operations with complete-content digests, artifact-owned identity checks, a 262,144-byte canonical limit, no dangling `state:objects/*` refs, and large/private bytes retained externally. Native Decision commits now bind a fresh team snapshot and WorkState, require the active canonical attempt created by authenticated selection, admit its Candidate-to-attempt-end continuation under expected-head Git CAS, reject stale reruns, resynchronize, and verify every accepted operation identity.
- Added guarded provider-neutral product release after canonical publication proof. Elected-host `ProductReleasePlan` binds one exact publication operation/revision, immutable artifact id/version/digest, release target/channel/destination, expected current channel revision/digest, adapter identity, and explicit user authority. Deterministic serialized effect jobs freeze scheduled identity, revalidate canonical publication and provider artifact state, require provider-key idempotency plus release-channel CAS, and re-observe exact operation/revision/digest before appending `runtime.product.released`. Digest-bound prepared/released manifests recover persisted promotions without attributing preexisting matching channels; ambiguous acceptance remains fail-closed. Adapter errors are redacted, credentials remain provider-owned, and release grants no rebuild, republish, deployment, Git tag, announcement, adoption, runtime-health, rollback, or business-outcome authority. Source tests use an injected fake provider; package metadata remains `"private": true` and no real release occurs.
- Added guarded provider-neutral product publication after canonical project-branch push proof. Elected-host `ProductPublicationPlan` binds one closed target, bounded private artifact, exact source commit/tree and SHA-256 digest, expected destination revision/digest, adapter identity, and explicit user authority. Deterministic serialized effect jobs freeze scheduled identity, reject symbolic or escaped artifact paths, revalidate canonical push proof and artifact bytes, require provider-key adapter idempotency on the deterministic job id plus adapter-enforced destination CAS, and re-observe exact provider operation, revision, and artifact digest before appending `runtime.product.published`. Digest-bound prepared/published manifests recover persisted operations without attributing preexisting matching artifacts; ambiguous acceptance before local operation evidence remains fail-closed. Adapter failures are redacted, credentials remain provider-owned, and publication grants no deployment, release, Git, tag, channel-promotion, registry-release, or business-outcome authority. Source-repository tests use an injected fake provider; package metadata remains `"private": true` and no real publication occurs.
- Added guarded project-branch push after canonical local merge proof. Only elected-host `ProjectBranchPushAuthority` with explicit user authority may bind one configured remote, exact local branch, and expected remote commit or branch absence. Deterministic serialized external-effect jobs revalidate canonical merge event/job identity, local commit/tree, coordinator generation, checked-out branch, CodeWiki-only dirtiness, safe credential-free configured remote URL, and exact remote head before issuing a structured normal non-force push with repository pre-push hooks disabled. Canonical `runtime.project_branch.pushed` events bind prior remote state, pushed commit/tree, merge identity, authority, and runtime job. Digest-bound private prepared/pushed manifests plus exact remote-state recovery cover the persisted-push-to-append window without attributing preexisting matching remote commits; abrupt death before the `pushed` phase persists remains fail-closed and unattributed. Remote drift, divergence, force options, embedded credentials, malformed output, policy-only authority, and unrelated dirtiness fail closed. Push proof grants no product publication, deployment, package release, registry, or business-outcome authority.
- Added guarded project-branch promotion after canonical Integration proof. An elected host may supply exact user or policy `ProjectBranchMergeAuthority` for one checked-out local branch; deterministic serialized effect jobs revalidate canonical Integration event/job identity, commit trailer, parent, tree, changed paths, patch digest, coordinator generation, checkout branch, allowed CodeWiki-only dirtiness, and current target commit before executing structured hook-disabled `git merge --ff-only`. Canonical `runtime.project_branch.merged` events bind prior commit, promoted commit/tree, Integration identity, authority, and runtime job. Exact-target recovery closes the merge-to-append crash window. Stale/non-fast-forward branches, unrelated dirty paths, detached/wrong branches, absent authority, and malformed Git results fail closed. Merge proof grants no push, publication, release, or remote authority.
- Added an opt-in OCI implementation-worker adapter behind the existing harness-neutral Assignment contract. Container-only hosts now receive exact `container` Assignments only after a bounded runtime availability probe; mutable image tags and unavailable runtimes are rejected before Claim append. The adapter uses structured Docker/Podman arguments, digest-pinned images, no implicit pulls, a read-only root, dropped capabilities, no privilege escalation, bounded memory/CPU/PIDs/output/time, explicit numeric identity, exact source/outcome mounts, canonical Git common metadata mounted read-only with fixed no-lock Git environment, no network by default, optional named restricted networks, and an explicit environment allowlist. Worktree Git admin metadata must resolve to the canonical repository before container start. The runtime client receives only a minimal local environment so ambient Docker/Podman remote-context variables cannot redirect execution. Cancellation terminates the foreground runtime client, force-removes the exact deterministic container, verifies that exact name is absent, waits for exit, and only then persists the same immutable recoverable Worker report used by process workers. Malformed, failed, timed-out, or cancelled outcomes cannot become Implementation acceptance; abrupt-death outcome scratch follows active-Claim and Integration-proof sanitation rules.
- Added guarded post-acceptance worker integration and exact Git proof. Deterministic target/base Integration jobs revalidate canonical Claim-bound Assignment packets, immutable Worker reports, Implementation acceptance, Planning path scopes, coordinator generation, and trace bytes; capture committed and untracked worker changes as bounded binary patches; apply them to private integration worktrees; run `git diff --check`; and create local no-GPG commits without moving the project checkout. Canonical `runtime.integration.proven` events bind exact job, Claim, Assignment, report, target, base/parent/commit/tree, changed paths, patch digest, and check evidence. Commit-trailer and in-progress-manifest recovery close pre-append crash windows, WorkState projects exact proof, and completed worker artifacts become cleanup-eligible only after matching proof.
- Added idempotent implementation-worker artifact sanitation. Reconciliation now matches private Assignment packets and Worker reports against canonical Claims, preserves active-Claim and unintegrated completed evidence, removes pre-Claim or terminal unsuccessful packet/report/output scratch, deletes runtime-local partial worktrees, and runs structured `git worktree prune`. Exact Integration proof authorizes completed packet/report/output/worktree cleanup; unknown, external-path, or cleanup-failed artifacts remain fail-safe instead of being deleted.
- Added cancellation-aware implementation worker draining. Graceful coordinator shutdown now aborts active Assignment jobs, propagates the exact abort signal into foreground Pi processes, sends `SIGTERM`, escalates to bounded `SIGKILL`, waits for child exit, persists an immutable `cancelled` Worker report, and routes the active Claim through the guarded terminal release path without creating Implementation acceptance.
- Added Product / Dictionary as the source-backed user-facing projection of `.codewiki/kb/lexicon.md`, with exact-term search, alphabetical navigation, stable contextual links, aliases, and deprecated-term guidance specified without creating a copied glossary. Expanded the canonical vocabulary contract to distinguish Work Items, Claims, Assignments, Assignment packets, Worker reports, and claim release by authority and lifecycle.
- Added automatic WorkState-to-claim-to-Assignment dispatch inside the elected project service. Authenticated bounded triggers cause the daemon to derive ready Work Items, enforce automation, agency, supervision, capacity, Git-base, dirty-path, and worktree-isolation policy, append exact claims with CAS, persist canonically digest-bound private Assignment packets, prepare explicit worktrees, and schedule non-conflicting workers without waiting for their completion. Replacement generations recover active claims through canonical packet digests and immutable Worker reports without trusting runtime scratch as authority. Exact matched reports are filtered to selected Work Items, bound into deterministic Implementation-review job identity, and supplied as candidate evidence only. Completed claims release after canonical Implementation acceptance; failed, blocked, or cancelled reports release without becoming implementation truth. Deterministic release jobs recheck active Assignment identity, generation ownership, and trace-byte CAS before append. Reconciliation removes safe pre-Claim and terminal unsuccessful artifacts while preserving active, unintegrated completed, and ambiguous evidence. Accepted completed output now enters guarded Integration scheduling, exact Git-proof-authorized cleanup, and separately authorized fast-forward project-branch promotion. Abrupt-death process observation, push/publication/release authority, trusted worker-image distribution, and external real-container/provider-auth proof remain gated.
- Added coordinator-scheduled implementation worker Assignments. A harness-neutral contract binds exact Assignment/claim/Work Item identity, Planning and trace refs, WorkState/source/context digests, scoped paths/components, prompt digest, report path, execution policy, and explicit worktree or container isolation. Typed coordinator jobs enforce one lane per Work Item, serialize hierarchical path overlap, run independent Assignments concurrently, and recover immutable Worker reports without reinvocation. The Pi daemon installs a compatibility process adapter over the existing worker path; it requires explicit worktrees, rejects report paths outside private runtime scratch or through symlinks, normalizes output, and atomically writes digest-bound reports.
- Added bounded generation-aware coordinator event delivery. Each elected service keeps a capped operational journal with monotonic cursors, authenticated leased long polling, bounded replay, explicit reset after retention gaps, and exact runtime-observed WorkState digests. Pi clients reconnect, compare generations, refresh routing after resets, and continue completed semantic work without trusting event payloads as truth. Dashboard observers reconnect to replacement generations and bridge coordinator invalidations into the browser state stream. Packed two-Pi/dashboard proof now observes cross-process disconnect delivery; peer-absent package smoke covers the same client API. Canonical Change Traces and project sources remain authoritative; the event journal is disposable.
- Added coordinator-owned Pi SDK semantic dispatch. The executable Pi daemon dynamically loads the optional entrypoint-isolated SDK adapter, advertises service-owned versus client-candidate execution, and runs exact Decision, Planning, or Implementation-review sessions behind authenticated trigger requests. Thin Pi clients hide semantic candidate tools when daemon execution is available; peer-absent packed installs remain operational with only the runtime-selected candidate fallback. Focused gates prove adapter loading, daemon-owned append execution, fallback capability, and clean shutdown without moving Pi SDK imports into harness-neutral runtime modules; the packed two-Pi/dashboard gate additionally proves discovery of the host Pi SDK across the installed package boundary.
- Added the detached project coordinator daemon and thin Pi/dashboard service clients. Pi sessions now use leased authenticated connections for runtime inspection, active semantic-tool routing, candidate-only submission to exact coordinator jobs, heartbeat, generation failover, and clean `session_shutdown`; dashboard runtimes register distinct observers. Removed the process-local reactor registry. A packed disposable gate starts two real Pi RPC processes plus one dashboard, proves all three share one coordinator generation with two approved supervisors, verifies execution pauses after both Pi clients exit, and confirms explicit shutdown leaves no daemon.
- Added exact semantic execution to the elected project coordinator service. Authenticated clients submit bounded triggers only; runtime derives compatible Planning and Implementation-review invariants, while Decision requires the separate authenticated exact-revision selection contract. Runtime maps admitted work to a typed lane and deterministic idempotency identity, revalidates fresh WorkState, executes one invariant without crossing lanes, and rechecks coordinator generation ownership before append. Successful semantic events carry the runtime-owned job id, allowing a replacement generation to recover exact trace event evidence without reinvoking the adapter. Focused and packed-package gates prove stale-selection rejection, pre-append fencing, remote execution, all-loop evidence binding, and no-reinvoke restart recovery.
- Added `@nunomoura/codewiki/coordinator`, a cross-process project-service host/client boundary around the coordinator kernel. It elects one live owner through an exclusive project lock, binds only to `127.0.0.1`, stores bearer endpoint metadata with current-user permissions, requires exact-generation request capabilities, rechecks ownership on every request, leases remote client registrations, rejects query-token access and unknown fields, replaces dead owners under new generations, and fences stale live sockets. Disposable multi-process and packed-package gates prove live-owner exclusion, two-client shared supervision, lease expiry/heartbeat, stale-generation rejection, unclean-process takeover, and cleanup.
- Added a transport-neutral `ProjectCoordinator` scheduling kernel with concurrent Pi/dashboard/CLI client registration, explicit supervised/unattended admission, deterministic typed lanes, resource-conflict holds, one Planning writer, one writer per integration target, bounded idempotency memory, mandatory durable recovery for canonical writes, and restart-safe recovery hooks. `ProjectServerReactor.selectProjectServerReactions()` now derives bounded compatible Planning and Implementation horizons, while authenticated exact-revision Decision selection enters a separate native revision-bound lane, preserving the singular executor as one job primitive. Focused and packed-package tests prove unrelated Decision and Work Item concurrency, overlapping-work holds, Planning/integration/effect serialization, supervision pause, duplicate suppression, and no duplicate durable write after restart.
- Added an entrypoint-isolated `@nunomoura/codewiki/pi-sdk` semantic-session adapter for bounded read-only Decision, Planning, and Implementation review. It accepts exactly one closed object candidate, limits invocation/candidate size and wall time, emits bounded lifecycle observations, disables discovered Pi resources, and blocks read-tool paths, globs, and symlinks that escape the project root. Unit and packed disposable-project gates prove the adapter without a model turn. Pi SDK remains an optional peer during the real model/auth architecture spike; process workers remain separate.
- Added canonical Product/System direction for one project-scoped CodeWiki control plane, purpose-built Work / Backlog / Planning / Implementation surfaces, Product / Users / Stories, canonical System diagrams, Design / Guidelines / UIs, and Change dossiers instead of per-Change pipelines.
- Added canonical JSONL Change Traces from first explicit persistence, with one stable Change-to-trace binding, Decision-loop intake/revision/status events, exact store/revision guards, and deterministic multi-Change batch identities. No hidden Git-ref store or compatibility importer remains.
- Added `WorkState`, a deterministic disposable project-wide projection over Change Traces that joins approved Changes, global Planning epochs, many-to-many Sprint membership, one owning Change per Work Item, Assignments, realization, blockers, incomplete multi-trace commits, next actions, and stable snapshot digests without creating another truth store.
- Added Pi-inspired streaming JSONL reads, incremental `WorkStateSession` tail refresh, and deterministic `ProjectServerReactor` selection with trigger-local priority, bounded linked/overlapping Planning horizons, and quiescence when no eligible semantic work exists. Pi active-tool routing now keeps archive and unrelated semantic-loop schemas out of model context, activating at most the runtime-selected loop adapter.
- Added digest-bound Live Preview profiles and canonical `uiPreviewTargets[]` with exact package-script and target content guards, Planning-owned target/profile bindings, Change/Sprint/Work Item correlation, profile-deduplicated native server supervision across routes, exact integration Git/tree/working-state digests, loopback readiness and attach behavior, automatic Implementation-stage startup, system/Playwright browser adapters, Pi-session cleanup, bounded redacted logs, source-harness reuse, and target-specific dashboard Open/Capture/Restart/Stop controls without adding a `/wiki-preview` command. Side-effect-free Playwright CLI preflight and verified browser-session state disable unsupported actions and provide explicit install/restart guidance without silently installing software. Explicit Playwright Capture writes accepted desktop/mobile screenshots plus bounded redacted console/network observations to a digest-correlated operational manifest tied to canonical UI target, contributing Changes, Sprint, Work Items, relevant Implementation iterations, and exact integration checkout state.
- Added a Google DESIGN.md-compatible `.codewiki/kb/product/DESIGN.md` to bootstrap output, with machine-readable visual tokens plus branding, typography, iconography, component, accessibility, and durable visual-reference guidance that remains valid CodeWiki OKF knowledge.
- Added a source-only `dashboard:dev` harness that requires a disposable external project, serves live-reloading dashboard assets without loading the CodeWiki extension, and opens the loopback dashboard through bounded system-browser or optional Playwright CLI adapters.
- Added deterministic quality-first execution policy with explicit provider/model/thinking propagation, pricing snapshots, tool and quality floors, bounded higher-quality escalation, token/cost/latency budgets, immutable autonomy ceilings, dashboard policy visibility, and fail-closed completion spend checks.
- Added guarded dashboard trace execution controls plus a supervised Trace Host result bridge that consumes Pi JSON events, retains only bounded sanitized outcomes, exposes approval digests without granting approval authority, captures resumable session identity and model/usage telemetry, resumes exact Pi sessions only through state/session guards and external-action acknowledgement, and fails closed on malformed, secret-shaped, or missing result envelopes.
- Added one strict declarative quality-pack schema for production and lab standards, with closed graph/evaluator/evidence identifiers, protected kernel standards, deterministic composition, and `observe`/`warn`/`enforce` rollout modes.
- Added Implementation Trace Detail worker-attempt observability, separate aggregate Integration and Exit Review, a deterministic human Activity Feed, and a bounded redacted Dev Log with failure retention and successful-close cleanup.
- Added dashboard runtime identity and endpoint health checks so stale loaded controllers and state-serving failures produce actionable restart/retry guidance instead of an unexplained blank shell.
- Added the CodeWiki lab for Decision, Planning, and Implementation exit
  condition scores (DEC, PEC, IEC), weighted quality standards, and candidate
  verification experiments.
- Added the lab holdout runner for sealed external evaluation bundles that live
  outside the repository and are not visible to candidate agents.
- Added the pipeline carryover lab (PCE) for testing decision-to-planning-to-
  implementation trace handoff fidelity with production-shaped trace events.
- Added `lab/program.md` and `npm run lab:objective` as the optimizer-facing
  quality-network program and scalar objective for visible/sealed lab scoring.
- Added versioned production loop quality graphs in `src/<loop>/loop.ts` and
  aligned lab candidates around graph ids, versions, layers, costs, and repair
  targets.
- Added `npm run lab:graph` for inspecting production/candidate graph shape,
  hashes, layers, and node deltas.
- Added `npm run lab:forge` for reducing trace JSONL into sanitized,
  human-labeled draft case material without treating raw traces as truth.
- Expanded visible DEC, PEC, and IEC seed corpora from 3 to 5 cases per loop.
- Hardened trace append helpers to validate records before writing, preventing
  stale generic semantic event names from corrupting hot trace files.
- Fixed trace-derived view projections to ignore superseded non-exit decision
  and planning attempts plus conflicts from already completed work units.
- Changed the `wiki_state` tool summary to report active work items rather than
  total historical work items.
- Added shared loop route metadata plus a direct implementation route for
  explicitly scoped, tiny/small low-risk decisions.
- Routed Planning and Implementation user-clarification/validation uncertainty
  back to Decision instead of blocking directly.
- Added a CodeWiki-owned loop graph runner with parallel node execution,
  hard-gate skip/fail-fast behavior, timeout diagnostics, and no Pi extension
  dependency.
- Added loop quality graph contract tests covering node method, gate,
  timeout, and per-node failure feedback across Decision, Planning, and
  Implementation.
- Added explicit hard-gate policy tests for binary semantic contracts in all
  three loop quality graphs.
- Added loop quality method policy tests for deterministic, agent-assessed,
  human-authority, and external-evidence standards.
- Added compact `qualityDiagnostics` repair feedback to loop results and trace
  outputs, sorted so hard-gate blockers appear before softer guidance.
- Added `npm run lab:promotion` to report promotion eligibility across visible
  gates, PCE, sealed holdout status, objective threshold, graph diff, and human
  review.
- Split loop graph identity from graph evaluation, added shared
  `src/loops/quality-standards.ts`, and routed production loop quality-standard
  evaluation through the per-loop `quality-standards.ts` modules.
- Wired `wiki_decide`, `wiki_plan`, and `wiki_implement` through the
  CodeWiki-owned loop runner for production quality-standard evaluation while
  preserving existing verdict and diagnostic semantics.
- Added independent quality-judge infrastructure for `agent_self_assessment` and
  `model_judge` standards with fake-judge tests, one batched call per loop
  attempt, graph/prompt/input-evidence cache keys, hard-gate skip behavior,
  versioned prompts, optional HTTP provider injection through config/env,
  `npm run lab:judge-calibration` for sealed judge calibration bundles,
  judge-calibration promotion gating in `npm run lab:promotion`, and
  `npm run lab:experiment` for isolated candidate worktree evaluation.
- Added `npm run lab:auto-experiment` for budgeted lab-only candidate sweeps with
  run, wall-clock, candidate-file, and diff-byte limits; score-only sealed
  feedback; best-candidate reporting; and no production graph mutation or
  automatic promotion.
- Added loop-level batched judge prompts for enabled production quality judges,
  including structured loop evidence, per-standard verdicts for semantic
  `model_judge` standards, prompt version `loop-quality-judge.v3`, and
  deterministic hard-gate prechecks before judge calls.
- Added quality-network judge-node metadata so each non-deterministic standard
  has its own specialized rubric and 0-100 judge score while still allowing
  batched transport per loop attempt.
- Added `npm run lab:judge-smoke` to validate a configured judge endpoint with
  synthetic non-private loop packets before sealed judge calibration.
- Added `npm run lab:sealed-template` to create off-repo starter templates for
  private holdout and judge calibration bundles without treating templates as
  sealed evidence.
- Added `npm run lab:sealed-check` to validate filled sealed bundles are
  off-repo, placeholder-free, and contain pass controls plus fail/block traps.
- Added lab loss v2 reason labels: fail/block cases can declare expected
  standard failures plus failure classes, so route-correct wrong-reason exits
  still lose score.
- Added reviewed self-dogfood baseline creation and verification with clean Git
  enforcement, Git tree content proof, package SHA-256 pinning, reviewer refs,
  and disposable state/config/decision-preview shadow execution that verifies
  source config and trace bytes remain unchanged.
- Added a tracked controller pin and fresh-clone installer that rebuilds the
  reviewed baseline commit in a detached worktree, requires an exact tarball
  byte count and SHA-256 match, and installs only that artifact under `.pi/npm`.

### Changed

- Clean-cut controlled implementation-worker execution to one authoritative Runtime path. Runtime now creates a worktree-bound prompt and exact Assignment, persists its packet, prepares and verifies the isolated worktree, and schedules one durable coordinator job before Managed Execution starts Pi. Removed the direct claim-to-session starter, manual Host handoff manifest and public types, helper-name completion/release protocol, failed-start compatibility release helper, tests, and packed artifacts without aliases. Every Implementation Worker Assignment now requires exact isolated-worktree custody before adapter invocation.
- Removed the final legacy `src/dashboard/**`, `tests/dashboard/**`, and packed `dist/dashboard/**` roots. Canonical Change/configuration reads and bounded App, activity, quality, worker, and Dev Log reductions now live under `src/project-server/queries/**`; Host App transport consumes `CodewikiAppState`; browser presentation remains under `src/clients/app/**`. Exported query/state names were clean-cut from Dashboard vocabulary without aliases or old-path re-exports.
- Removed projection-authored Activity Feed impact and next-action narration. The App now renders existing bounded factual trace activities directly, so presentation no longer invents aggregate-review or Integration causality; no legacy `activityFeed` field or old packed artifact remains.
- Moved browser App HTTP transport and endpoint lifecycle to `src/host/app/**`, including request validation, coordinator observation, event delivery, Preview command handling, and focused Host integration tests. Clean-cut exported server/request-error names and private temporary-directory configuration to App vocabulary without old-path exports; the later Runtime App query cut removed the remaining `src/dashboard/**` files.
- Made browser App Change and execution-configuration surfaces read-only until typed Runtime commands exist. Removed Dashboard-local Change/configuration mutation controls, Add/Revise/Validate/Withdraw and Save controls, and both POST command routes. The App continues to present bounded Change facts and the effective configuration snapshot without claiming mutation authority.
- Removed the App-to-ambient-Pi session action bridge. Browser `Resume`, `Change`, and `Resolve Blocker` controls, `/api/session-actions/commands`, session-action state/receipts, Pi `sendUserMessage` injection, and both old packed artifacts are absent without replacement aliases. Explicit user-invoked `/wiki-*` commands remain; future App mutations must use typed Runtime commands rather than prompt injection.
- Moved App daemon bootstrap and installed-package identity checks to `src/host/app/**`, renamed their APIs and private daemon environment vocabulary to Host/App terms, and removed the obsolete `src/dashboard/index.ts` barrel without aliases. Pi Clients originally imported the still-legacy service module directly; the later Host App transport cut moved that service under Host ownership.
- Established the browser-first App shell under `src/clients/app/**`, including its packaged logo asset and browser contract test. Host App transport now serves this App artifact directly; old `src/dashboard/assets*` and `dist/dashboard/assets/**` paths are absent. Deleted the unused standalone Dashboard Change-validation renderer and its isolated test instead of carrying test-only product code into the App.
- Removed the final generic Harness source and package root. OCI container execution, linked-worktree Git validation, isolation options, cancellation, report persistence, and cleanup proof now live under `src/project-server/workbenches/container/**`; the coordinator package composition entrypoint now lives under `src/host/**`. The public `./coordinator` subpath remains but resolves only to the new Host artifact, while package gates require the entire old `dist/harnesses/**` tree to be absent.
- Moved neutral execution ports and every managed Pi adapter from former `src/harnesses/**` ownership to `src/runtime/**`, with matching tests and packed `./pi-sdk` exports. Public `HarnessCapability*`, invocation-option, and Repair Harness Invocation names are now `ExecutionCapability*`, execution invocation options, and `RepairExecutionInvocation` without aliases or old-path re-exports. Runtime may import only neutral `execution/ports.ts`; concrete Pi adapters cannot import Clients.
- Removed conversational Pi `wiki_decide`, `wiki_plan`, and `wiki_implement` Candidate tools plus ambient session-driven Runtime routing. Pi now exposes bounded reads and explicit user commands only; exact `/wiki-select` starts authenticated Decision through Runtime, while managed semantic execution remains isolated behind Pi SDK execution ports. `wiki_state` retains semantic next-action facts without advertising nonexistent tool names.
- Ratified source-backed User Standards as the only project-specific assurance input. Public verification vocabulary now separates User Standards, Default/Custom Check origin, Code/Model evaluation, and Loop applicability. Company policy, execution guidance, quality criteria, and resource instructions are Standard content rather than separate artifacts. Target distillation reports Default Check coverage and proposes atomic Custom Model or approved-template Custom Code Checks with exact source passages; hard resource Code Checks may drive matching Runtime guards, while non-pass/fail preferences compile into protected deterministic Backlog Triage behavior. Backlog Triage remains a recommendation; authenticated exact-revision user selection is required before Decision starts, while Planning alone orders executable Work Items. The source-unbound Custom Check path and automatic pending-Change Decision selection are now removed without aliases or dual paths.
- Collapsed the pre-release Worker receipt / Worker result split into one immutable Worker report with explicit attempt status. Runtime now persists, recovers, validates, and submits the same report through `workerReports`, `reportPath`, `reportRef`, and `runtime-worker-report:*` contracts while keeping canonical Implementation acceptance separate.
- Reframed Pi ownership: CodeWiki owns project orchestration and client projections; Pi provides conversational clients plus entrypoint-isolated semantic-session and process-worker execution adapters. Compatible independent invariants may run concurrently while Decision revision, project Planning, Work Item claim, integration, commit, and publication lanes remain explicitly serialized where required.
- Replaced the intended Change-rooted dashboard pipeline with separate Backlog intake, project Planning graph, Implementation execution cockpit, Product, System, Design, and cross-cutting Change dossier contracts. The existing production dashboard remains implementation drift pending the control-plane and multi-session external spike.
- Moved semantic-loop invocation behind `runProjectServerSemanticExecutor()`. Runtime now selects one eligible owner, injects canonical entity and append authority, enforces iteration/wall-clock/CAS budgets, reruns stale observations, stops on route-back, and repeats committed progress to quiescence. Pi semantic tools now submit judgment or evidence candidates instead of calling loop facades with caller-marshalled repository facts.
- Removed caller-marshalled Implementation authority. `runWikiImplement()` now resolves runtime-selected Sprint, Work Items, owning Change, Planning events, Assignments, source ownership, trace parent, sequence, and byte guards from canonical project state; host runners group worker evidence by canonical Work Item ownership.
- Reframed CodeWiki as one supervised event-driven runtime outer loop around exactly three quality-governed semantic loops. Change is now the accountable intent carrier; Decision is approval of an exact Change revision rather than a separate entity; Planning owns Sprint creation across the relevant approved-Change portfolio; one Change may span several Sprints and one Sprint may coordinate several Changes; Sprint and Backlog state are views rather than separate truth roots.
- Cut Change APIs, feedback intake, dashboard Change reads, and Decision acceptance over to JSONL Change Traces, then removed the hidden Git-ref constants, readers, migration adapters, fixtures, and compatibility tests in a clean pre-release cut.
- Selected `@nunomoura/codewiki` as the eventual npm identity while retaining `"private": true` so npm publication remains blocked during stabilization.
- Advanced the reproducible self-dogfood controller to `v0.3.9` at commit `a04aca6`, package SHA-256 `b13f58bb48715af3ef9bb1c60f67da73c3ee0f8c6072a554b505f145c50ae5dd`, with bounded model-visible focused state and direct closure sequencing.
- Advanced the reproducible self-dogfood controller to `v0.3.8` at commit `0c003d9`, package SHA-256 `48a07c29b86c759d63745cdab58ed54bac18944e8f588d7ffc5cc2665c342d29`, with guarded trace-id archive closure and exact budget-stop attribution.
- Advanced the reproducible self-dogfood controller to `v0.3.7` at commit `f3955ec`, package SHA-256 `83698ea3fe491bdab6220bbda237809a7897f9ffdff95ccdacaa4cbe09948c2b`, with corrected startup-failure classification and runtime-temp readiness evidence.
- Advanced the reproducible self-dogfood controller to `v0.3.6` at commit `f87088c`, package SHA-256 `0b1837165ab04a1433a32e9ae54c4ec06591d88be637169b4abe6440f3eb6b2e`, with reviewed execution-policy routing and accepted-Change shadow evidence.
- Migrated all production semantic-loop standards to immutable enforcing kernel packs. Current graph identities version with current contracts; stale pre-release contracts receive no compatibility projection.
- Migrated all lab candidates to observe-only lab packs and exposed pack identity in graph reports without granting candidates production or controller authority.
- Hardened the Decision lab candidate with a deterministic specificity standard,
  raising DEC to 100 against the locked seed cases.
- Hardened the Planning lab candidate with deterministic work-unit specificity
  and path-scope overlap standards, raising PEC to 100 against the locked seed
  cases.
- Hardened the Implementation lab candidate with a deterministic evidence
  specificity standard, raising IEC to 100 against the locked seed cases and
  making `npm run lab:gate` pass.
- Renamed the canonical production and lab quality-graph files to
  `loop.ts`.
- Upgraded loop quality graph schema metadata to include explicit methods,
  binary/soft gates, and timeout budgets.
- Hardened Decision, Planning, and Implementation graph gate classification so
  core route, coverage, authority, traceability, scope, and evidence failures are
  hard gates.
- Classified Implementation check, TDD, and content-proof standards as
  `external_evidence` instead of deterministic semantic checks.
- Changed deterministic production quality nodes from binary-only scores to
  partial 0-100 scores based on activated issue coverage while preserving
  fail-closed route status and hard-gate behavior.
- Changed generated Git worktree operations from shell command strings to
  structured executable-and-argument commands, with runtime handoff schema v2.
  Explicit project setup commands remain host-approved shell commands.
- Upgraded quality graph schema to v3 with validated node dependencies, cycle
  rejection, and dependency-aware runner scheduling.
- Disabled mutable-source CodeWiki loading; after packed external, shadow,
  reproducible installer, and explicit approval gates passed, supervised
  repo-local autoload now targets only the reviewed controller under `.pi/npm`.

### Fixed

- Added bounded model-visible focused `wiki_state` data and a direct closure sequence so Trace Hosts receive closability and append guards without reconstructing state through repository searches.
- Added guarded `traceId`-based archive close resolution and closure-host guidance so agents no longer consume entire raw trace files before calling `wiki_archive`.
- Reported the exact elapsed-time, latency, route-timeout, token, or monetary limit that stopped a supervised Trace Host instead of labeling every budget stop as elapsed-time exhaustion.
- Allowed the documented ignored `.codewiki/runtime/tmp/**` operational root in readiness checks without treating it as active workflow truth or permitting other runtime roots.
- Preserved authoritative Trace Host process failures when economic budgets are configured instead of masking missing-credential or startup failures as missing-usage budget blockers.
- Migrated the pinned-controller shadow gate to seed and preview an exact validated Change acceptance instead of calling the removed authored-proposal Decision input.
- Cleared loop-standard timeout handles after node settlement and attributed
  skipped standards to their actual failed dependency.
- Restricted dashboard endpoint metadata, bearer tokens, and daemon logs to the
  current OS user on POSIX hosts.
- Prevented dashboard keyboard shortcuts and trace toggles from interfering with
  nested controls, while adding keyboard focus and expansion state to trace cards.
- Updated the packed-package smoke contract for the dashboard shutdown hook.
- Rejected unknown config keys at every nested config boundary instead of
  silently discarding misspelled policy fields.
- Moved the dashboard launch token from the request URL into a fragment-backed
  session capability, added restrictive browser headers, and removed duplicate
  trace-file reads from dashboard refreshes.
- Kept open blocked traces visible in the dashboard Active facet while preserving
  their simultaneous visibility in Blocked.
- Made open dashboards stream trace appends immediately and recover through
  bounded one-second polling when event delivery is missed or disconnected, then
  advanced the reviewed pinned controller baseline to serve both fixes.

### Notes

- Visible lab 100% is no longer treated as meaningful proof by itself; promotion
  requires PCE coverage, objective scoring, an external holdout gate, and review.
- App benchmarks remain deferred until the lab-proven standards are reviewed and
  promoted into production loop exits.

## [0.3.0] - 2026-06-22

### Added

- Added the production-readiness gate for the Pi package: package install smoke, Pi RPC smoke, Pi mutation smoke, project-local install smoke, external lifecycle smoke, external failure smoke, readiness checklist, npm audit, and diff hygiene.
- Added trace-first runtime backend support for hosts, heartbeat cycles, trigger run planning, work-unit claim selection, leases, worker starts, worker report collection, and runtime board visibility.
- Added exact Pi extension surfaces for direct slash commands (`/wiki-state`, `/wiki-resume`, `/wiki-explain`, `/wiki-config`, `/wiki-bootstrap`) and model-facing tools (`wiki_state`, `wiki_config`, `wiki_decide`, `wiki_plan`, `wiki_implement`, `wiki_archive`).
- Added private pre-release package metadata and local pack/install gates for future npm distribution readiness.

### Changed

- Kept the package private and documented that the future npm registry package name is TBD because the unscoped `codewiki` name is already owned by another maintainer.
- Changed semantic trace output events from generic loop iteration names to split `loop` plus specific `event` facts such as `decision.rows_approved`, `planning.work_units_created`, and `implementation.evidence_accepted`.
- Kept runtime coordination events under `runtime.*` without semantic `loop` fields.
- Made `wiki_state` trace-derived only and kept source ownership in the KB source map and `/wiki-explain` path.
- Updated package documentation to avoid advertising a public npm install before the package is ready to publish.

### Removed

- Removed the repo-local CodeWiki extension shim and repo-local dogfood gate.
- Removed the grouped `/wiki ...` slash namespace in favor of direct `/wiki-*` commands.
- Removed the `_OLD_VERSION/**` archive after completing migration audit and production-readiness cleanup.

### Validation

- `npm run audit:codewiki` passed before this release preparation.
- `npm view codewiki` showed the unscoped package name belongs to another maintainer; no public publish target is selected yet.

## [0.1.2] - 2026-05-28

### Added

- Initial changelog baseline for the early scaffold package line.

### Notes

- Earlier development occurred before this changelog was introduced.
- The package remains private during current distribution-readiness work.
